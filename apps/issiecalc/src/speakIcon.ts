/**
 * The audio identity of IssieCalc: the voice settings tab and the calculator's
 * read-aloud button share this glyph and colour, so anything that speaks is
 * recognisable wherever it appears.
 *
 * Kept in its own module rather than on a screen, so neither screen has to
 * import the other just to stay in sync.
 */
export const SPEAK_ICON_NAME = 'volume-high';
export const SPEAK_ICON_COLOR = '#059669';
