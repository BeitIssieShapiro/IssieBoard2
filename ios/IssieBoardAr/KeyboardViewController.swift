import UIKit

/**
 * Arabic Keyboard Extension Controller
 * Inherits from BaseKeyboardViewController and configures itself for Arabic.
 */
class KeyboardViewController: BaseKeyboardViewController {
    // The keyboard language is automatically detected from Info.plist's KeyboardLanguage key
    // which is set to "ar" for this keyboard extension
}