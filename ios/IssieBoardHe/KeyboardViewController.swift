import UIKit

/**
 * Hebrew Keyboard Extension Controller
 * Inherits from BaseKeyboardViewController and configures itself for Hebrew.
 */
class KeyboardViewController: BaseKeyboardViewController {
    // The keyboard language is automatically detected from Info.plist's KeyboardLanguage key
    // which is set to "he" for this keyboard extension
}