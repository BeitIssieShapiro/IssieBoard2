import UIKit

/**
 * English Keyboard Extension Controller
 * Inherits from BaseKeyboardViewController and configures itself for English.
 */
class KeyboardViewController: BaseKeyboardViewController {
    // The keyboard language is automatically detected from Info.plist's KeyboardLanguage key
    // which is set to "en" for this keyboard extension
}